<header>
  <div class="container">
    <div class="site-name">
      share
    </div>
    <%= form_with url: search_products_path, method: :get, local: true do |f| %>
      <%= f.text_field :key %>
      <%= f.submit "検索" %>
    <% end %>
  </div>
</header>